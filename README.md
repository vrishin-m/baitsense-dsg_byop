Baitsense is an AI-Based browser extension that detects and flags clickbait videos on youtube



SETUP:

run the following commands on your terminal:

pip install numpy

pip install pandas

pip install scikitlearn

pip install matplotlib

pip install torch

pip install tensorflow

pip install joblib

pip install deepface

pip install sentencetransformer

pip install xgboost 

